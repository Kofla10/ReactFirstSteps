import React, {Fragment} from "react";

const TextTest = ({text}) => {
    return (
        <Fragment>
            <p className="acrd-text">{text}</p>
        </Fragment>
    )
}

export default TextTest;